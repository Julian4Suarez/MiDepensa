# Third-party images shared by every environment.
#
# Pinned by digest so a deploy can never pick up a different build of the same
# tag. Refresh with:
#   docker pull postgres:16-alpine
#   docker inspect --format '{{index .RepoDigests 0}}' postgres:16-alpine
POSTGRES_IMAGE=postgres:16-alpine
