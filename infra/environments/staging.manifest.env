# Application images deployed to staging.
#
# Replace the tags with the immutable image references produced by your build
# (for example ghcr.io/<owner>/midepensa-backend:sha-<commit>).
BACKEND_IMAGE=midepensa-backend:staging
FRONTEND_IMAGE=midepensa-frontend:staging
